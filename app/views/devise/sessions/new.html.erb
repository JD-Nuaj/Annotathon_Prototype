<section class="flex justify-center items-center min-h-screen bg-cover bg-center" style="background-image: url('https://issues.fr/wp-content/uploads/2023/12/Lenergie-du-plancton-liberer-la-capacite-cachee-de-stockage-du.jpg');">
  <div class="max-w-sm w-full p-8 bg-white bg-opacity-70 rounded-lg shadow-xl">
    <h2 class="text-3xl font-semibold text-center text-gray-800 mb-6">Sign In</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <!-- Email field -->
      <div class="mb-5">
        <%= f.label :email, class: "block text-base font-medium text-gray-700" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "mt-2 block w-full sm:w-80 px-3 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 text-sm" %>
      </div>

      <!-- Password field -->
      <div class="mb-5">
        <%= f.label :password, class: "block text-base font-medium text-gray-700" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "mt-2 block w-full sm:w-80 px-3 py-2 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 text-sm" %>
      </div>

      <!-- 'Remember me' checkbox -->
      <% if devise_mapping.rememberable? %>
        <div class="mb-5 flex items-center">
          <%= f.check_box :remember_me, class: "h-5 w-5 text-indigo-600 border-gray-300 rounded" %>
          <%= f.label :remember_me, class: "ml-2 text-sm text-gray-700" %>
        </div>
      <% end %>

      <!-- Sign in button -->
      <div class="mb-5">
        <%= f.submit "Sign In", class: "w-full py-2 px-4 bg-indigo-600 text-white font-semibold rounded-lg shadow-lg hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 text-sm" %>
      </div>

    <% end %>

    <!-- Devise links -->
    <div class="text-center mt-4">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</section>
